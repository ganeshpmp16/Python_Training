# sample to use main() function as entry point of the program
def main():
    print("This is the main function. It is the entry point of the program.")
if __name__ == "__main__": # This condition checks if the script is being run directly (as the main program) or imported as a module in another script
    main() # This will call the main() function when the script is run directly